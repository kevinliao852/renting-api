# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_DIALECT=postgres
DB_USERNAME=your_username
DB_PASSWORD=your_password
DB_NAME=rental_db

# JWT Configuration
JWT_SECRET=your_jwt_secret
JWT_EXPIRATION_TIME=3600 # Token expiration time in seconds

# Server Configuration
PORT=3000

# Logging Configuration
LOG_LEVEL=debug # Set to 'debug', 'info', 'warn', 'error', or 'silent' as per your preference

# Other Configuration Options (if applicable)
# Any additional configuration variables specific to your application, such as API keys, third-party service URLs, etc.

